# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'first-ios-app' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for first-ios-app
    pod 'Firebase/Core'
    pod 'Firebase/Database'

  target 'first-ios-appTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'first-ios-appUITests' do
    # Pods for testing
  end

end
